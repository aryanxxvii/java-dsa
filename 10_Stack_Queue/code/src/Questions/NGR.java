package Questions;

public class NGR {
    // move from right to left
    // if stack is empty -> ans for that element is -1
    // else, pop till greater number is found
    // put that element in the stack
}
