package Questions;

public class StockSpan {
    // same as Nearest Greater to Left
    // Move from left to right
    // for each element, pop the stack till greater number is found
    // if stack gets empty, -1
    // use index values instead of actual numbers in stack, to find span
}
