package Questions;

public class CelebrityProblem {
    // Put all elements in a stack
    // Take first two elements from stack
    // Check if A knows B, and if B knows A
    // Put the possible celebrity back in the stack
    // Last element remaining can be a celebrity
    // Check it separately
}
