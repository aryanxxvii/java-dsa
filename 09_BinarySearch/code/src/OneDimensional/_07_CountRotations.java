package OneDimensional;

public class _07_CountRotations {
    public static void main(String[] args) {

    }

    public static void countRotations() {
        // Index of minimum index = number of rotations
        // When 'ans' is updated (min(arr[X], ans)), keep track of X in 'index variable'
        /*
            if (arr[low] <= arr[mid]) {
                if (arr[low] < ans) {
                    index = low;
                    ans = arr[low];
                }
            }
         */
    }

}
